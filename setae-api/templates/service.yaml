apiVersion: v1
kind: Service
metadata:
  name: {{ include "setea-api.fullname" . }}
  labels:
    {{- include "setea-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.containerPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "setea-api.selectorLabels" . | nindent 4 }}
